!!!
%html{lang: "en"}
  %head
    %meta{charset: "UTF-8"}
    %title Phototrack
    %meta{name: "viewport", content: "width=device-width, initial-scale=1.0, shrink-to-fit=no"}
    = csrf_meta_tags
    = csp_meta_tag
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload'
    = javascript_include_tag 'application', 'data-turbolinks-track': 'reload'
    %body
      %header#header
        %h1 Phototrack
        -if current_user
          %p.greeting= "Welcome, #{current_user.name}"

      = yield

    %footer#footer
      %p= link_to "Log out", logout_url
      %p= link_to "Users", reporters_path
      %p= link_to "Shoots", shoots_path
      %small Site design by Olivia Dimmer.
